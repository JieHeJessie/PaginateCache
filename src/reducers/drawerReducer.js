const initialState = {
	showDrawer: false,
	cardDetails: {}
}

export default function reducer(state = initialState, action){
	switch (action.type) {
		case label_1:
			// statements_1
			break;
		default:
			// statements_def
			break;
	}
}